#include <iostream>
#include <vector>
#include "landscape.h"

void SampleSimulator(std::vector<LandscapeCell> land_v);

int main() {

    // Get current landscape state (Simulator). A state is defined t
    // Select action (Agent)
    // Get Simulator result
    // Store this (state from the simulator and action taken).
    // This will be used by a learning agent which next time will try to select the best action

    return 0;
}

void SampleSimulator(std::vector<LandscapeCell> land_v) {
    // TODO (Elizabeth)

}
