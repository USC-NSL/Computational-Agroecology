#include <iostream>
#include <vector>
#include "landscape.h"

void SampleSimulator(vector<LandscapeCell> land_v);
void InitialLandConfig(int no_of_cells, double latitude, double longitude);
int main() {

    // Get current landscape state (Simulator)
    // Select action (Agent)
    // Simulate action...prints action selected at defined timesteps (Simulator)

    return 0;
}

void SampleSimulator(vector<LandscapeCell> land_v) {
    // TODO (Elizabeth)

}
void InitialLandConfig(int no_of_cells, double latitude, double longitude) {

    // TODO (Elizabeth)

}
